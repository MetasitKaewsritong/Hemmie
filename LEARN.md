# 🎵 Music School Management System

## เกี่ยวกับโปรเจคนี้

โปรเจคนี้คือ **ระบบจัดการโรงเรียนดนตรี** ที่สร้างด้วย **Laravel Framework**

สิ่งที่ระบบทำได้:
- ✅ จัดการข้อมูล **นักเรียน** (Students)
- ✅ จัดการข้อมูล **อาจารย์ผู้สอน** (Instructors)
- ✅ จัดการข้อมูล **เครื่องดนตรี** (Instruments)
- ✅ จัดการข้อมูล **หลักสูตร** (Courses)
- ✅ จัดการข้อมูล **บทเรียน** (Lessons)
- ✅ จัดการข้อมูล **การลงทะเบียน** (Enrollments)
- ✅ จัดการข้อมูล **การชำระเงิน** (Payments)
- ✅ จัดการข้อมูล **การแสดง** (Performances)

---

## 🛠️ เทคโนโลยีที่ใช้

| เทคโนโลยี | ใช้ทำอะไร |
|-----------|----------|
| **Laravel 12** | Framework หลักของ PHP |
| **PHP 8.2** | ภาษาโปรแกรมฝั่ง Backend |
| **Blade** | Template Engine สำหรับสร้างหน้าเว็บ |
| **Bootstrap 5** | CSS Framework ทำให้หน้าเว็บสวยงาม |
| **MySQL** | ฐานข้อมูล |
| **Vite** | Build tool สำหรับ Frontend |

---

## 📁 โครงสร้างไฟล์ (File Structure)

```
โปรเจคนี้มี 3 ส่วนหลักที่ต้องรู้:

📁 project/
│
├── 📁 routes/                    ← 1️⃣ ROUTES (เส้นทาง URL)
│   └── web.php                      กำหนดว่า URL ไหน ไปที่ไหน
│
├── 📁 app/Http/Controllers/      ← 2️⃣ CONTROLLERS (ตัวควบคุม)
│   ├── StudentsController.php       รับคำสั่ง → ทำงาน → ส่งผลลัพธ์
│   ├── CoursesController.php
│   └── ...อื่นๆ
│
└── 📁 resources/views/           ← 3️⃣ VIEWS (หน้าเว็บ)
    ├── layout.blade.php             Template หลัก
    ├── students/                    หน้าจัดการนักเรียน
    │   ├── index.blade.php          - หน้าแสดงรายการ
    │   ├── create.blade.php         - หน้าฟอร์มเพิ่ม
    │   └── edit.blade.php           - หน้าฟอร์มแก้ไข
    └── ...อื่นๆ
```

---

## 🎯 หลักการ MVC คืออะไร?

**MVC** = **M**odel + **V**iew + **C**ontroller

### เปรียบเทียบกับร้านอาหาร 🍽️

```
┌─────────────────────────────────────────────────────────────────────┐
│                    🍽️ เปรียบเทียบ MVC กับร้านอาหาร                     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   👤 ลูกค้า (User)                                                   │
│      │                                                              │
│      │ "ขอข้าวผัดหนึ่งจาน"                                             │
│      ▼                                                              │
│   📋 พนักงานเสิร์ฟ (ROUTES)                                           │
│      │                                                              │
│      │ รับออเดอร์ → ส่งต่อให้ครัว                                       │
│      ▼                                                              │
│   👨‍🍳 พ่อครัว (CONTROLLER)                                            │
│      │                                                              │
│      │ รับออเดอร์ → หยิบวัตถุดิบ → ปรุงอาหาร → จัดจาน                     │
│      ▼                                                              │
│   🍳 ตู้เย็น/วัตถุดิบ (MODEL/DATABASE)                                  │
│      │                                                              │
│      │ เก็บวัตถุดิบ (ข้อมูล)                                           │
│      ▼                                                              │
│   🍽️ จานอาหาร (VIEW)                                                  │
│      │                                                              │
│      │ นำเสนอให้ลูกค้า                                                 │
│      ▼                                                              │
│   👤 ลูกค้าได้รับอาหาร (User เห็นหน้าเว็บ)                              │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### ในโปรเจคนี้:

| ส่วนประกอบ | ไฟล์ | หน้าที่ |
|------------|------|--------|
| **Routes** | `routes/web.php` | รับ URL → ส่งต่อให้ Controller |
| **Controller** | `app/Http/Controllers/*.php` | ประมวลผล → ดึงข้อมูล → ส่งไป View |
| **View** | `resources/views/*.blade.php` | แสดงผลหน้าเว็บให้ user เห็น |
| **Database** | MySQL | เก็บข้อมูลทั้งหมด |

---

## 🔄 การทำงานทีละขั้นตอน

### ตัวอย่าง: เมื่อ User เข้าหน้า `/students`

```
┌─────────────────────────────────────────────────────────────────────┐
│  ขั้นตอนที่ 1: User พิมพ์ URL                                         │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                     │
│   👤 User พิมพ์: http://localhost:8000/students                      │
│                                                                     │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│  ขั้นตอนที่ 2: Routes รับ URL                                         │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                     │
│   📁 routes/web.php                                                  │
│                                                                     │
│   Route::resource('students', StudentsController::class);           │
│                                                                     │
│   แปลว่า: ถ้า URL = /students → ไปที่ StudentsController             │
│                                                                     │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│  ขั้นตอนที่ 3: Controller ทำงาน                                       │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                     │
│   📁 app/Http/Controllers/StudentsController.php                     │
│                                                                     │
│   public function index()                                           │
│   {                                                                 │
│       // ดึงข้อมูลนักเรียนทั้งหมดจาก Database                           │
│       $students = DB::table('Students')->get();                     │
│                                                                     │
│       // ส่งข้อมูลไปแสดงที่ View                                       │
│       return view('students/index', compact('students'));           │
│   }                                                                 │
│                                                                     │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│  ขั้นตอนที่ 4: View แสดงผล                                            │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                     │
│   📁 resources/views/students/index.blade.php                        │
│                                                                     │
│   @foreach ($students as $student)                                  │
│       <tr>                                                          │
│           <td>{{ $student->first_name }}</td>                       │
│           <td>{{ $student->last_name }}</td>                        │
│       </tr>                                                         │
│   @endforeach                                                       │
│                                                                     │
│   👤 User เห็นตารางรายชื่อนักเรียน!                                    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 📍 Route Resource อธิบาย

เมื่อเขียน `Route::resource('students', StudentsController::class);` 

Laravel จะสร้าง **7 routes** ให้อัตโนมัติ:

```
┌──────────────────────────────────────────────────────────────────────────┐
│                        Route::resource สร้างอะไรให้บ้าง?                    │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  HTTP     URL                      Method        ทำอะไร                   │
│  ───────────────────────────────────────────────────────────────────────│
│  GET      /students                index()       แสดงรายการทั้งหมด          │
│  GET      /students/create         create()      แสดงฟอร์มเพิ่มข้อมูล        │
│  POST     /students                store()       บันทึกข้อมูลใหม่            │
│  GET      /students/{id}           show()        แสดงรายละเอียด 1 รายการ    │
│  GET      /students/{id}/edit      edit()        แสดงฟอร์มแก้ไข             │
│  PUT      /students/{id}           update()      อัพเดทข้อมูล               │
│  DELETE   /students/{id}           destroy()     ลบข้อมูล                  │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 🔗 ความสัมพันธ์ Route → Controller → View

### แผนภาพความสัมพันธ์ทั้งหมด

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              routes/web.php                                  │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  Route::resource('students', StudentsController::class);              │  │
│  │  Route::resource('courses', CoursesController::class);                │  │
│  │  Route::resource('instructors', InstructorsController::class);        │  │
│  │  Route::resource('instruments', InstrumentsController::class);        │  │
│  │  Route::resource('lessons', LessonsController::class);                │  │
│  │  Route::resource('enrollments', EnrollmentsController::class);        │  │
│  │  Route::resource('payments', PaymentsController::class);              │  │
│  │  Route::resource('performances', PerformancesController::class);      │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
          ┌───────────────────────────┼───────────────────────────┐
          │                           │                           │
          ▼                           ▼                           ▼
┌─────────────────────┐   ┌─────────────────────┐   ┌─────────────────────┐
│ StudentsController  │   │ CoursesController   │   │ InstructorsController│
│─────────────────────│   │─────────────────────│   │─────────────────────│
│ index()             │   │ index()             │   │ index()             │
│ create()            │   │ create()            │   │ create()            │
│ store()             │   │ store()             │   │ store()             │
│ edit()              │   │ edit()              │   │ edit()              │
│ update()            │   │ update()            │   │ update()            │
│ destroy()           │   │ destroy()           │   │ destroy()           │
└─────────┬───────────┘   └─────────┬───────────┘   └─────────┬───────────┘
          │                         │                         │
          ▼                         ▼                         ▼
┌─────────────────────┐   ┌─────────────────────┐   ┌─────────────────────┐
│ views/students/     │   │ views/courses/      │   │ views/instructors/  │
│─────────────────────│   │─────────────────────│   │─────────────────────│
│ index.blade.php     │   │ index.blade.php     │   │ index.blade.php     │
│ create.blade.php    │   │ create.blade.php    │   │ create.blade.php    │
│ edit.blade.php      │   │ edit.blade.php      │   │ edit.blade.php      │
│ show.blade.php      │   │ show.blade.php      │   │ show.blade.php      │
└─────────────────────┘   └─────────────────────┘   └─────────────────────┘
```

### ตารางสรุป

| URL | Controller | Method | View |
|-----|------------|--------|------|
| `/students` | StudentsController | index() | students/index.blade.php |
| `/students/create` | StudentsController | create() | students/create.blade.php |
| `/students/{id}/edit` | StudentsController | edit() | students/edit.blade.php |
| `/courses` | CoursesController | index() | courses/index.blade.php |
| `/courses/create` | CoursesController | create() | courses/create.blade.php |
| `/instructors` | InstructorsController | index() | instructors/index.blade.php |
| ... | ... | ... | ... |

---

## 📝 CRUD คืออะไร?

**CRUD** = **C**reate, **R**ead, **U**pdate, **D**elete

หรือก็คือ 4 การกระทำหลักกับข้อมูล:

```
┌─────────────────────────────────────────────────────────────────────┐
│                         CRUD Operations                              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   C = Create   →   เพิ่มข้อมูลใหม่         →   POST /students        │
│   R = Read     →   ดูข้อมูล               →   GET /students         │
│   U = Update   →   แก้ไขข้อมูล             →   PUT /students/{id}    │
│   D = Delete   →   ลบข้อมูล               →   DELETE /students/{id} │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## ➕ CREATE: การเพิ่มข้อมูล

### Flow การเพิ่มข้อมูลนักเรียนใหม่

```
┌─────────────────────────────────────────────────────────────────────┐
│  Step 1: กดปุ่ม "เพิ่มข้อมูล"                                          │
└──────────────────────────────────┬──────────────────────────────────┘
                                   │
   <a href="/students/create">     │
      เพิ่มข้อมูล                   │    GET /students/create
   </a>                            │
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Step 2: Controller แสดงฟอร์ม                                        │
│                                                                     │
│  StudentsController::create()                                       │
│  → return view('students/create');                                  │
└──────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Step 3: User กรอกฟอร์มและกด Submit                                  │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  ชื่อ: [สมชาย          ]                                      │   │
│  │  นามสกุล: [ใจดี        ]                                      │   │
│  │  อีเมล: [somchai@email.com]                                  │   │
│  │                                                              │   │
│  │  [💾 บันทึก]                                                  │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  <form action="/students" method="POST">                            │
│      @csrf                                                          │
│      ...                                                            │
│  </form>                                                            │
└──────────────────────────────────┬──────────────────────────────────┘
                                   │    POST /students
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Step 4: Controller บันทึกลง Database                                │
│                                                                     │
│  StudentsController::store()                                        │
│                                                                     │
│  DB::table('Students')->insert([                                    │
│      'first_name' => 'สมชาย',                                        │
│      'last_name'  => 'ใจดี',                                         │
│      'email'      => 'somchai@email.com',                           │
│  ]);                                                                │
│                                                                     │
│  → redirect ไปหน้า /students พร้อมข้อความ "Success"                  │
└──────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Step 5: กลับไปหน้ารายการ พร้อมข้อความสำเร็จ ✅                        │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  ✅ Student created successfully.                            │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  รหัส  │  ชื่อ    │  นามสกุล │  อีเมล                          │   │
│  │───────│─────────│─────────│────────────────────────────────│   │
│  │  001  │  สมชาย   │  ใจดี    │  somchai@email.com             │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## ✏️ UPDATE: การแก้ไขข้อมูล

### Flow การแก้ไขข้อมูลนักเรียน

```
┌─────────────────────────────────────────────────────────────────────┐
│  Step 1: กดปุ่ม "แก้ไข" ที่แถวนั้น                                     │
└──────────────────────────────────┬──────────────────────────────────┘
                                   │    GET /students/1/edit
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Step 2: Controller ดึงข้อมูลเดิมมาแสดงในฟอร์ม                         │
│                                                                     │
│  StudentsController::edit($id)                                      │
│  {                                                                  │
│      $student = DB::table('Students')                               │
│                   ->where('student_id', $id)                        │
│                   ->get();                                          │
│      return view('students/edit', compact('student'));              │
│  }                                                                  │
└──────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Step 3: User แก้ไขข้อมูลและกด Submit                                 │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  ชื่อ: [สมชาย ✏️        ]                                      │   │
│  │  นามสกุล: [รักเรียน ✏️  ]   ← แก้จาก "ใจดี" เป็น "รักเรียน"       │   │
│  │  อีเมล: [somchai@email.com]                                  │   │
│  │                                                              │   │
│  │  [💾 อัพเดท]                                                  │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  <form action="/students/1" method="POST">                          │
│      @csrf                                                          │
│      @method('PUT')    ← บอก Laravel ว่าเป็น PUT request             │
│      ...                                                            │
│  </form>                                                            │
└──────────────────────────────────┬──────────────────────────────────┘
                                   │    PUT /students/1
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Step 4: Controller อัพเดท Database                                  │
│                                                                     │
│  StudentsController::update($request, $id)                          │
│                                                                     │
│  DB::table('Students')                                              │
│      ->where('student_id', $id)                                     │
│      ->update([                                                     │
│          'first_name' => 'สมชาย',                                    │
│          'last_name'  => 'รักเรียน',   ← อัพเดทค่าใหม่                 │
│      ]);                                                            │
│                                                                     │
│  → redirect ไปหน้า /students                                        │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🗑️ DELETE: การลบข้อมูล

### Flow การลบข้อมูล

```
┌─────────────────────────────────────────────────────────────────────┐
│  Step 1: กดปุ่ม "ลบ"                                                  │
│                                                                     │
│  <form action="/students/1" method="POST">                          │
│      @csrf                                                          │
│      @method('DELETE')    ← บอก Laravel ว่าเป็น DELETE request       │
│      <button>🗑️ ลบ</button>                                         │
│  </form>                                                            │
└──────────────────────────────────┬──────────────────────────────────┘
                                   │    DELETE /students/1
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Step 2: Controller ลบข้อมูล                                          │
│                                                                     │
│  StudentsController::destroy($id)                                   │
│  {                                                                  │
│      DB::table('Students')                                          │
│          ->where('student_id', $id)                                 │
│          ->delete();                                                │
│                                                                     │
│      return redirect()->route('students.index')                     │
│                       ->with('Success', 'Deleted successfully');    │
│  }                                                                  │
└──────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Step 3: กลับไปหน้ารายการ + ข้อมูลหายไป ✅                             │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🎨 Blade Template อธิบาย

### Blade คืออะไร?

**Blade** คือ Template Engine ของ Laravel ใช้สร้างหน้า HTML

ไฟล์ Blade จะลงท้ายด้วย `.blade.php`

### โครงสร้าง Layout

```
┌─────────────────────────────────────────────────────────────────────┐
│                      layout.blade.php                                │
│                     (Template หลัก)                                   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   <!DOCTYPE html>                                                   │
│   <html>                                                            │
│   <head>                                                            │
│       <title> @yield('title') </title>    ← ช่องว่างสำหรับ title       │
│   </head>                                                           │
│   <body>                                                            │
│       ┌─────────────────────────────────────────────────────────┐  │
│       │  🔴 NAVBAR                                               │  │
│       │  [Main] [Students] [Courses] [Instructors] ...          │  │
│       └─────────────────────────────────────────────────────────┘  │
│                                                                     │
│       ┌─────────────────────────────────────────────────────────┐  │
│       │                                                          │  │
│       │           @yield('content')                              │  │
│       │                                                          │  │
│       │           ← ช่องว่างสำหรับเนื้อหา                           │  │
│       │             แต่ละหน้าจะใส่เนื้อหาต่างกัน                     │  │
│       │                                                          │  │
│       └─────────────────────────────────────────────────────────┘  │
│   </body>                                                           │
│   </html>                                                           │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
                                    ▲
                                    │ @extends('layout')
         ┌──────────────────────────┼──────────────────────────┐
         │                          │                          │
         ▼                          ▼                          ▼
┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
│ students/       │      │ courses/        │      │ instructors/    │
│ index.blade.php │      │ index.blade.php │      │ index.blade.php │
├─────────────────┤      ├─────────────────┤      ├─────────────────┤
│ @extends(layout)│      │ @extends(layout)│      │ @extends(layout)│
│                 │      │                 │      │                 │
│ @section(title) │      │ @section(title) │      │ @section(title) │
│   Students      │      │   Courses       │      │   Instructors   │
│ @endsection     │      │ @endsection     │      │ @endsection     │
│                 │      │                 │      │                 │
│ @section(content│      │ @section(content│      │ @section(content│
│   <table>...</>  │      │   <table>...</>  │      │   <table>...</>  │
│ @endsection     │      │ @endsection     │      │ @endsection     │
└─────────────────┘      └─────────────────┘      └─────────────────┘
```

### Blade Directives ที่ใช้บ่อย

| Directive | ความหมาย | ตัวอย่าง |
|-----------|---------|---------|
| `@extends('layout')` | สืบทอดจาก template หลัก | ทุกหน้าจะมี navbar เหมือนกัน |
| `@section('title')` | กำหนดค่าให้ช่อง title | `@section('title', 'Students')` |
| `@yield('content')` | ช่องว่างให้หน้าลูกใส่เนื้อหา | อยู่ใน layout.blade.php |
| `{{ $variable }}` | แสดงค่าตัวแปร | `{{ $student->name }}` |
| `@foreach` | วน loop | วนแสดงข้อมูลในตาราง |
| `@if` / `@endif` | เงื่อนไข | แสดง message ถ้ามี |
| `@csrf` | ป้องกัน attack | ใส่ในทุก form |
| `@method('PUT')` | กำหนด HTTP method | สำหรับ Update/Delete |

---

## 🗄️ Database Query

### การดึงข้อมูลจาก Database

โปรเจคนี้ใช้ **Query Builder** (ไม่ใช้ Eloquent ORM)

```php
// ═══════════════════════════════════════════════════════════════════
// SELECT - ดึงข้อมูล
// ═══════════════════════════════════════════════════════════════════

// ดึงข้อมูลทั้งหมด
$students = DB::table('Students')->get();
// SQL: SELECT * FROM Students

// ดึงข้อมูลตามเงื่อนไข
$student = DB::table('Students')
            ->where('student_id', 1)
            ->get();
// SQL: SELECT * FROM Students WHERE student_id = 1

// ═══════════════════════════════════════════════════════════════════
// INSERT - เพิ่มข้อมูล
// ═══════════════════════════════════════════════════════════════════

DB::table('Students')->insert([
    'student_id' => 1,
    'first_name' => 'สมชาย',
    'last_name' => 'ใจดี',
]);
// SQL: INSERT INTO Students (...) VALUES (...)

// ═══════════════════════════════════════════════════════════════════
// UPDATE - แก้ไขข้อมูล
// ═══════════════════════════════════════════════════════════════════

DB::table('Students')
    ->where('student_id', 1)
    ->update([
        'first_name' => 'สมหญิง',
    ]);
// SQL: UPDATE Students SET first_name = 'สมหญิง' WHERE student_id = 1

// ═══════════════════════════════════════════════════════════════════
// DELETE - ลบข้อมูล
// ═══════════════════════════════════════════════════════════════════

DB::table('Students')
    ->where('student_id', 1)
    ->delete();
// SQL: DELETE FROM Students WHERE student_id = 1
```

---

## 🔗 Entity Relationship (ความสัมพันธ์ของข้อมูล)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         Database Relationships                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│    ┌──────────────┐                                                         │
│    │ Instruments  │       เครื่องดนตรี (กีต้าร์, เปียโน, กลอง...)              │
│    │──────────────│                                                         │
│    │ instrument_id│                                                         │
│    │ name         │                                                         │
│    └──────┬───────┘                                                         │
│           │                                                                 │
│           │ 1:N (1 เครื่องดนตรี มีได้หลายหลักสูตร)                             │
│           │                                                                 │
│           ▼                                                                 │
│    ┌──────────────┐      ┌──────────────┐      ┌──────────────┐            │
│    │   Courses    │      │ Instructors  │      │   Students   │            │
│    │──────────────│      │──────────────│      │──────────────│            │
│    │ course_id    │      │ instructor_id│      │ student_id   │            │
│    │ course_name  │      │ first_name   │      │ first_name   │            │
│    │ instrument_id│      │ instrument_id│      │ last_name    │            │
│    │ level        │      │ salary       │      │ email        │            │
│    │ price        │      └──────┬───────┘      └──────┬───────┘            │
│    └──────┬───────┘             │                     │                    │
│           │                     │                     │                    │
│           └──────────┬──────────┴─────────────────────┘                    │
│                      │                                                      │
│                      ▼                                                      │
│               ┌──────────────┐                                              │
│               │ Enrollments  │   การลงทะเบียน                                │
│               │──────────────│   (นักเรียน + หลักสูตร + อาจารย์)               │
│               │ enrollment_id│                                              │
│               │ student_id   │ ← FK                                         │
│               │ course_id    │ ← FK                                         │
│               │ instructor_id│ ← FK                                         │
│               │ start_date   │                                              │
│               │ status       │                                              │
│               └──────┬───────┘                                              │
│                      │                                                      │
│      ┌───────────────┼───────────────┐                                     │
│      │               │               │                                      │
│      ▼               ▼               ▼                                      │
│ ┌──────────┐  ┌──────────┐  ┌───────────────┐                              │
│ │ Lessons  │  │ Payments │  │ Performances  │                              │
│ │──────────│  │──────────│  │───────────────│                              │
│ │lesson_id │  │payment_id│  │performance_id │                              │
│ │enroll_id │  │enroll_id │  │enrollment_id  │                              │
│ │lesson_   │  │amount    │  │performance_   │                              │
│ │  date    │  │payment_  │  │  date         │                              │
│ │topic     │  │  date    │  │score          │                              │
│ └──────────┘  └──────────┘  └───────────────┘                              │
│                                                                             │
│  บทเรียน        การชำระเงิน      การแสดง/สอบ                                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🚀 วิธีรันโปรเจค

### 1. ติดตั้ง Dependencies

```bash
# ติดตั้ง PHP packages
composer install

# ติดตั้ง Node.js packages
npm install
```

### 2. ตั้งค่า Environment

```bash
# สร้างไฟล์ .env
cp .env.example .env

# สร้าง Application Key
php artisan key:generate
```

### 3. รันโปรเจค

```bash
# รันทุกอย่างพร้อมกัน (Server + Vite)
composer dev

# หรือรันแยก:
php artisan serve    # รัน Laravel server
npm run dev          # รัน Vite
```

### 4. เข้าใช้งาน

เปิด Browser: **http://localhost:8000**

---

## 📖 สรุป

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              สรุปการทำงาน                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│    1. User พิมพ์ URL                                                        │
│           │                                                                 │
│           ▼                                                                 │
│    2. Routes (web.php) → จับคู่ URL กับ Controller                          │
│           │                                                                 │
│           ▼                                                                 │
│    3. Controller → ดึงข้อมูลจาก Database + ส่งไป View                        │
│           │                                                                 │
│           ▼                                                                 │
│    4. View (Blade) → สร้าง HTML + แสดงผล                                    │
│           │                                                                 │
│           ▼                                                                 │
│    5. User เห็นหน้าเว็บ!                                                    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                           ไฟล์ที่ต้องรู้จัก                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   📁 routes/web.php              →  กำหนด URL                               │
│   📁 app/Http/Controllers/       →  Business Logic                          │
│   📁 resources/views/            →  หน้าเว็บ (HTML)                          │
│   📁 resources/views/layout.blade.php  →  Template หลัก                     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔌 ไฟล์เชื่อมกันยังไง? (File Connections)

### แผนภาพรวม

```
┌──────────────────────────────────────────────────────────────────────────────────────┐
│                                                                                      │
│   👤 User                     HTTP Protocol                                          │
│   Browser  ─────────────────────────────────────────────────────►  Laravel App       │
│            GET /students                                                             │
│                                                                                      │
└──────────────────────────────────────────────────────────────────────────────────────┘
```

---

### 📍 การเชื่อมต่อทีละขั้น

#### **1. Route → Controller**

| ไฟล์ | บรรทัด | คำสั่ง |
|-----|-------|-------|
| `routes/web.php` | Line 13 | `Route::resource('students', StudentsController::class);` |

```php
// routes/web.php (Line 13)
Route::resource('students', StudentsController::class);
```

**อธิบาย:**
- `Route::resource()` = สร้าง 7 routes อัตโนมัติ
- `'students'` = URL path (`/students`)
- `StudentsController::class` = Controller ที่จะรับงาน

**HTTP Methods ที่สร้าง:**
| Method | URL | → Controller Method |
|--------|-----|---------------------|
| `GET` | `/students` | `index()` |
| `GET` | `/students/create` | `create()` |
| `POST` | `/students` | `store()` |
| `GET` | `/students/{id}/edit` | `edit()` |
| `PUT` | `/students/{id}` | `update()` |
| `DELETE` | `/students/{id}` | `destroy()` |

---

#### **2. Controller → Database**

| ไฟล์ | บรรทัด | คำสั่ง |
|-----|-------|-------|
| `app/Http/Controllers/StudentsController.php` | Line 12 | `DB::table('Students')->get();` |

```php
// app/Http/Controllers/StudentsController.php

public function index()  // Line 10
{
    $students = DB::table('Students')->get();  // Line 12 - ดึงข้อมูลจาก DB
    return view('students/index', compact('students'));  // Line 13 - ส่งไป View
}
```

**อธิบาย:**
- `DB::table('Students')` = เลือกตาราง `Students`
- `->get()` = ดึงข้อมูลทั้งหมด (SELECT *)
- `compact('students')` = ส่งตัวแปร `$students` ไปยัง View

---

#### **3. Controller → View**

| ไฟล์ | บรรทัด | คำสั่ง |
|-----|-------|-------|
| `app/Http/Controllers/StudentsController.php` | Line 13 | `return view('students/index', compact('students'));` |

```php
return view('students/index', compact('students'));
//          ↑               ↑
//          │               └── ส่งตัวแปร $students ไปด้วย
//          └── ไปที่ไฟล์ resources/views/students/index.blade.php
```

**การแปลง path:**
| `view('...')` | → ไฟล์จริง |
|---------------|-----------|
| `'students/index'` | `resources/views/students/index.blade.php` |
| `'students/create'` | `resources/views/students/create.blade.php` |
| `'layout'` | `resources/views/layout.blade.php` |

---

#### **4. View (Child) → Layout (Parent)**

| ไฟล์ | บรรทัด | คำสั่ง |
|-----|-------|-------|
| `resources/views/students/index.blade.php` | Line 1 | `@extends('layout')` |

```php
// resources/views/students/index.blade.php

@extends('layout')              // Line 1 - สืบทอดจาก layout.blade.php
@section('title', 'Students')   // Line 2 - กำหนด title
@section('content')             // Line 3 - เริ่มเนื้อหา

    @foreach ($students as $student)   // วนแสดงข้อมูล
        {{ $student->first_name }}     // แสดงชื่อ
    @endforeach

@endsection                     // จบเนื้อหา
```

**การเชื่อม:**
```
layout.blade.php                 index.blade.php
─────────────────               ───────────────────
@yield('title')    ◄─────────── @section('title', 'Students')
@yield('content')  ◄─────────── @section('content') ... @endsection
```

---

#### **5. View → Route (Links/Forms)**

| ไฟล์ | บรรทัด | คำสั่ง |
|-----|-------|-------|
| `resources/views/students/index.blade.php` | Line 8 | `{{ route('students.create') }}` |

```html
<!-- resources/views/students/index.blade.php -->

<!-- ปุ่ม Link (GET request) -->
<a href="{{ route('students.create') }}">เพิ่มข้อมูล</a>
<!--        ↑
            └── สร้าง URL: /students/create
                Method: GET
-->

<!-- ปุ่ม Edit (GET request) -->
<a href="{{ route('students.edit', $student->student_id) }}">แก้ไข</a>
<!--        ↑
            └── สร้าง URL: /students/1/edit
                Method: GET
-->

<!-- Form Submit (POST request) -->
<form action="{{ route('students.store') }}" method="POST">
    @csrf    <!-- Token ป้องกัน CSRF attack -->
    ...
</form>
<!--    ↑
        └── ส่งไปที่ URL: /students
            Method: POST
-->

<!-- Form Delete (DELETE request) -->
<form action="{{ route('students.destroy', $id) }}" method="POST">
    @csrf
    @method('DELETE')   <!-- บอก Laravel ว่าเป็น DELETE -->
    <button>ลบ</button>
</form>
<!--    ↑
        └── ส่งไปที่ URL: /students/1
            Method: DELETE (ผ่าน POST + @method)
-->
```

---

### 📊 สรุปการเชื่อมต่อทั้งหมด

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                                                                                     │
│  ┌─────────────────┐    Route::resource()    ┌──────────────────────────────────┐  │
│  │  routes/web.php │ ─────────────────────► │ app/Http/Controllers/            │  │
│  │                 │                         │ StudentsController.php           │  │
│  │ Line 13:        │                         │                                  │  │
│  │ Route::resource │                         │ Line 12: DB::table()->get()     │  │
│  │ ('students',    │                         │ Line 13: return view(...)       │  │
│  │  StudentsCtrl)  │                         │                                  │  │
│  └─────────────────┘                         └───────────────┬──────────────────┘  │
│                                                              │                     │
│                                                              │ view('students/     │
│                                                              │       index')       │
│                                                              ▼                     │
│  ┌─────────────────┐    @extends('layout')   ┌──────────────────────────────────┐  │
│  │ resources/views/│ ◄───────────────────── │ resources/views/students/        │  │
│  │ layout.blade.php│                         │ index.blade.php                  │  │
│  │                 │                         │                                  │  │
│  │ @yield('content')◄─────────────────────── │ @section('content')              │  │
│  │                 │                         │   {{ $student->name }}           │  │
│  │ Navbar + Footer │                         │ @endsection                      │  │
│  └─────────────────┘                         └───────────────┬──────────────────┘  │
│                                                              │                     │
│                                                              │ <a href="{{ route   │
│                                                              │ ('students.create') │
│         ┌────────────────────────────────────────────────────┘ }}">                │
│         │                                                                          │
│         │  HTTP GET /students/create                                               │
│         ▼                                                                          │
│  ┌─────────────────┐                                                               │
│  │  routes/web.php │  → วนกลับไปที่ Controller อีกครั้ง                               │
│  │                 │                                                               │
│  └─────────────────┘                                                               │
│                                                                                     │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

---

### 🎯 Quick Reference: คำสั่งเชื่อมไฟล์

| จาก | ไป | คำสั่ง | Protocol/Method |
|-----|-----|-------|-----------------|
| **Route → Controller** | `web.php` → Controller | `Route::resource('students', StudentsController::class)` | - |
| **Controller → DB** | Controller → MySQL | `DB::table('Students')->get()` | TCP/MySQL |
| **Controller → View** | Controller → Blade | `return view('students/index', compact('students'))` | - |
| **View → Layout** | Blade → Layout | `@extends('layout')` | - |
| **View → Route (Link)** | Blade → URL | `<a href="{{ route('students.create') }}">` | HTTP GET |
| **View → Route (Form)** | Blade → URL | `<form action="{{ route('...') }}" method="POST">` | HTTP POST |
| **Form Delete** | Blade → URL | `@method('DELETE')` | HTTP DELETE* |
| **Form Update** | Blade → URL | `@method('PUT')` | HTTP PUT* |

> *\*HTML forms รองรับแค่ GET/POST ดังนั้น Laravel ใช้ `@method()` เพื่อ simulate PUT/DELETE*

---

### 🔢 Route Names ที่ใช้ใน `route()`

```php
Route::resource('students', StudentsController::class);
// สร้าง route names:

route('students.index')    // → /students           (GET)
route('students.create')   // → /students/create    (GET)
route('students.store')    // → /students           (POST)
route('students.show', 1)  // → /students/1         (GET)
route('students.edit', 1)  // → /students/1/edit    (GET)
route('students.update',1) // → /students/1         (PUT)
route('students.destroy',1)// → /students/1         (DELETE)
```

---

*Made with ❤️ for learning Laravel*
